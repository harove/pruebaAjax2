<%= form_with(model: [company, complain]) do |f| %>
    <%= f.label :content %>
    <%= f.text_field :content %>
    <%= f.submit %>
<% end %>

<ul id= "company_complains_list">
    <% company.complains.reverse_each do |c| %>
        <li><%= c.content %></li>
    <% end %>
</ul>